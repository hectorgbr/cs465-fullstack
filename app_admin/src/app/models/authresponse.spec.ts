import { AuthResponse } from './authresponse';

describe('Authresponse', () => {
  it('should create an instance', () => {
    // the fuction goes with "R" for Response
    expect(new AuthResponse()).toBeTruthy();
  });
});